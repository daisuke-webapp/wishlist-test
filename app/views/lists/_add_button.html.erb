    <%= form_with(model: current_user.favorites.build) do |f| %>
      <%= hidden_field_tag :image_url, ['mediumImageUrls'][0] %>
      <%= hidden_field_tag :name, item.name %>
      <%= hidden_field_tag :url, item.url %>
      <%= hidden_field_tag :price, item.price %>
      <%= hidden_field_tag :item_code, item.code %>
      <%= f.submit 'Add Your Wishlist', class: 'mx-1 btn btn-success btn-sm' %>
    <% end %>
  <% end %>